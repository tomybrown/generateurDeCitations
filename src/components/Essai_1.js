const choix = (l) =>l[Math.random()*l.lenght | 0] || ""
	const generateur=(n,taille=200)=> {
	const dic={}
const J = Object.keys(lyrics);
const I = Object
	.getElementbyId("lyrics")('p')[0].innerhtml;
const G = [...J].map((_,k)=>[I.slice(k,k+n,I[k+n]]);
const mots = I.split('  ').filter(m=>m.lenght>=n);
mots[Math.random()*mots.lenght | 0].slice(0,3);
var creatlyrics=mots[Math.random()*mots.lenght | 0].slice(0,n)
G.foreach(t=>
	dic.hasOwnProperty(t[0]) ?
	dic[t[0]].push(t[1]): 
	dic[t[0]] = [t[1]])
for (var i=0;i<taille;i++) creatlyrics+=choix(dic[creatlyrics.slice(-n)])
return creatlyrics;
}
}
